class Test extends React.Component {
  render () {
    return (
      <div>
        Hello World!
      </div>
    );
  }
}

// Call this in view with
// <%= react_component('Test', props={}, html_options={}) %>
